# Структура проекта

## 📁 Организация папок

```
mainweb/
├── assets/                 # Статические ресурсы для сборки
│   ├── css/               # Глобальные стили
│   │   └── main.css       # Основные стили Tailwind
│   ├── fonts/             # Шрифты (опционально)
│   └── images/            # Изображения для импорта (опционально)
│
├── components/            # Vue компоненты
│   ├── sections/          # Секции страниц
│   │   ├── AboutSection.vue
│   │   ├── ContactSection.vue
│   │   └── ProjectsSection.vue
│   └── ui/                # UI компоненты
│       ├── Footer.vue
│       └── Navigation.vue
│
├── composables/           # Композиции Vue (переиспользуемая логика)
│   └── useAnimations.ts   # Логика анимаций
│
├── layouts/               # Layout компоненты
│   └── default.vue        # Базовый layout
│
├── pages/                 # Страницы приложения (автоматический роутинг)
│   ├── index.vue          # Главная страница (/)
│   ├── offer.vue          # Публичная оферта (/offer)
│   ├── privacy.vue        # Политика конфиденциальности (/privacy)
│   └── refund.vue         # Политика возврата (/refund)
│
├── public/                # Публичные файлы (копируются как есть)
│   ├── 404/               # Ресурсы для страницы 404
│   │   └── content/
│   │       ├── cat.png
│   │       └── okak.mp3
│   └── src/               # Статические ресурсы
│       ├── avatar.jpg
│       └── favicon.ico
│
├── types/                 # TypeScript типы
│   └── index.ts           # Определения типов
│
├── utils/                 # Утилиты и константы
│   ├── animations.ts      # Конфигурация анимаций
│   └── constants.ts       # Константы (проекты, контакты, статистика)
│
├── app.vue                # Корневой компонент приложения
├── error.vue              # Страница ошибки 404
├── nuxt.config.ts         # Конфигурация Nuxt
├── tailwind.config.js     # Конфигурация Tailwind CSS
├── tsconfig.json          # Конфигурация TypeScript
├── package.json           # Зависимости проекта
├── .gitignore            # Игнорируемые файлы для Git
├── .npmrc                # Конфигурация npm
└── README.md             # Документация проекта
```

## 🗂️ Описание папок

### `assets/`
Статические ресурсы, которые обрабатываются во время сборки (минификация, оптимизация).

### `components/`
Переиспользуемые Vue компоненты:
- **sections/** - Секции для страниц (Projects, About, Contact)
- **ui/** - UI компоненты (Navigation, Footer)

### `composables/`
Композиции Vue - переиспользуемая логика, которая может использоваться в компонентах.

### `layouts/`
Layout компоненты для обертки страниц.

### `pages/`
Страницы приложения. Nuxt автоматически создает роуты на основе структуры папок.

### `public/`
Публичные файлы, которые копируются в корень сборки без изменений.

### `types/`
TypeScript типы для типобезопасности.

### `utils/`
Утилиты и константы:
- **constants.ts** - Все данные (проекты, контакты, статистика)
- **animations.ts** - Конфигурация анимаций

## 🔄 Удаленные файлы

Следующие старые файлы были удалены:
- ❌ `index.html` (старый HTML файл)
- ❌ `404/index.html` (старый HTML файл)
- ❌ `offer/index.html` (старый HTML файл)
- ❌ `privacy/index.html` (старый HTML файл)
- ❌ `refund/index.html` (старый HTML файл)
- ❌ `js/main.js` (старый JS файл)
- ❌ `js/tailwind-config.js` (дубликат конфига)
- ❌ Папки `js/`, `404/`, `offer/`, `privacy/`, `refund/`, `src/` (старые папки)

## ✅ Улучшения структуры

1. **Организация компонентов** - Компоненты разделены на `sections/` и `ui/`
2. **Централизованные константы** - Все данные в `utils/constants.ts`
3. **Типизация** - TypeScript типы в `types/`
4. **Утилиты** - Конфигурация анимаций в `utils/animations.ts`
5. **Layout система** - Базовый layout в `layouts/default.vue`

## 📝 Добавление нового проекта

Чтобы добавить новый проект, отредактируйте `utils/constants.ts`:

```typescript
export const projects: Project[] = [
  // ... существующие проекты
  {
    id: 'new-project',
    title: 'Новый проект',
    description: 'Описание проекта',
    tags: ['Тег1', 'Тег2'],
    link: 'https://example.com',
    icon: 'fas fa-icon',
    gradient: 'from-blue-500 to-purple-500'
  }
]
```

## 🎨 Настройка стилей

Глобальные стили находятся в `assets/css/main.css`. Конфигурация Tailwind в `tailwind.config.js`.

## 🚀 Запуск проекта

```bash
npm install  # Установка зависимостей
npm run dev  # Запуск dev сервера
```

